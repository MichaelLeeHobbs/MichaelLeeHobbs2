'use strict';

describe('myApp.webdevres module', function() {

  beforeEach(module('myApp.webdevres'));

  describe('webdevres controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var webdevresCtrl = $controller('WebdevresCtrl');
      expect(webdevresCtrl).toBeDefined();
    }));

  });
});