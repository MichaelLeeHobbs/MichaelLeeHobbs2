'use strict';

describe('myApp.connect module', function() {

  beforeEach(module('myApp.connect'));

  describe('connect controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var connectCtrl = $controller('ConnectCtrl');
      expect(connectCtrl).toBeDefined();
    }));

  });
});